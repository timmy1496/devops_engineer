# Віртуалізація з VirtualBox — Ubuntu LTS TestVM

## Етапи виконання

### 1. Встановлення VirtualBox

- Завантажено та встановлено останню версію VirtualBox з офіційного сайту:  
  [https://www.virtualbox.org](https://www.virtualbox.org)
- Перевірено успішність встановлення: VirtualBox відкривається без помилок.

---

### 2. Створення нової віртуальної машини

- Створено нову VM з параметрами:
    - **Назва:** `TestVM`
    - **Тип:** `Linux`
    - **Версія:** `Ubuntu (64-bit)`
    - **ОЗУ:** 2 ГБ
    - **Жорсткий диск:** 20 ГБ у форматі `VDI`

---

### 3. Налаштування віртуальної машини

- Встановлено **2 процесорні ядра**
- Увімкнено **EFI** (Enable EFI: special OSes only)
- Мережевий адаптер налаштовано на **Bridged Adapter** — VM отримує IP з локальної мережі

---

### 4. Інсталяція операційної системи

- Завантажено останній LTS ISO-образ Ubuntu з офіційного сайту:  
  [https://ubuntu.com/download/desktop](https://ubuntu.com/download/desktop)
- Підключено ISO до VM як оптичний диск
- Виконано повну установку Ubuntu на віртуальний диск

---

### 5. 💾 Робота зі знімками (Snapshots)

- Створено **знімок після налаштування системи**
- Виконано зміни в системі (створення тестового файлу test.yaml)
- Здійснено **відновлення зі знімку** — зміни успішно скасовано (файл test.yaml відсутній)

---

### 6. Зміна параметрів VM (через GUI VirtualBox)

#### 📏 Збільшення розміру жорсткого диску:
- VM вимкнено
- Через VBoxManage та PowerShell збільшено розмір VDI-диску з 20 ГБ до **30 ГБ**
- Усередині Ubuntu:
    - Перевірено збільшений обсяг
    - Розширено файлову систему

#### Зміна CPU та RAM:
- Змінено кількість процесорних ядер до **4**
- Змінено обсяг оперативної пам'яті до **4 ГБ**
- Перевірено, що зміни застосовано коректно
- Дані на диску збережені

---

### 7. Завершення роботи з VM

- **Коректно вимкнено Ubuntu** через систему (GUI / `sudo shutdown now`)
- Повністю **видалено VM** з VirtualBox:
    - Через GUI обрано опцію **Delete all files**
    - Видалено всі пов’язані файли: `.vbox`, `.vdi`, snapshots

---
